numeros = {1, 2, 3, 2}

numeros = list(numeros) # convertendo numeros(conjunto) ppara lista.

print(numeros[0])

# conjuntos em python não possuem INDEXAÇÃO e nem FATIAMENTO, por isso é necessário converter para listas para executar esses métodos.