frutas = (
    "laranja",
    "pera",
    "uva",  # se os valores da tupla forem passados pelo parenteses, necessa´rio colocar uma virgula no final para não confundir com PRESCEDÊNCIA.
)
print(frutas)

letras = tuple("python") # iterando as letras e jogando na variável
print(letras)

numeros = tuple([1, 2, 3, 4]) # convertendo uma lista para tupla
print(numeros)

pais = ("Brasil",)
print(pais)
