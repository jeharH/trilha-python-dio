cores = (
    "vermelho",
    "azul",
    "verde",
    "azul",
)

print(cores.count("vermelho"))  # 1
print(cores.count("azul"))  # 2
print(cores.count("verde"))  # 1

# um dos únicos métodos para TUPLAS (devido a elas serem IMUTÁVEIS), é o count que conta o o número de elementos da tupla